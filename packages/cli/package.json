{
  "name": "@ruood/announcement-cli",
  "version": "0.1.0",
  "description": "Headless announcement authoring and publishing. Every operation the UI will perform is a command here first.",
  "license": "UNLICENSED",
  "main": "dist/main.js",
  "bin": { "announce": "dist/bin.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "test": "jest"
  },
  "dependencies": {
    "@ruood/announcement-core": "0.1.0",
    "@ruood/announcement-schema": "0.1.0"
  }
}
