{
  "name": "@ruood/announcement-core",
  "version": "0.1.0",
  "description": "Build, image pipeline and git publishing for the announcements repository.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json",
    "test": "jest"
  },
  "dependencies": {
    "@ruood/announcement-authoring": "0.1.0",
    "@ruood/announcement-schema": "0.1.0",
    "sharp": "^0.34.2",
    "simple-git": "^3.27.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "js-yaml": "^5.4.1"
  }
}
